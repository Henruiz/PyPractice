Class Node:

def init(self, value)
    self.value = value
    self.next = None


class SLL:

    def init(self)
        self.head = None


    def printLL(self)
        print_value = value
        if print_value is not None
            print(print_value)
            print_value = print.value.next


    def add_at_front(self, value)
        NewNode = Node(value)

        NewNode.next = self.head
        self.head = NewNode


# def add_at_end(self, value)
#     NewNode = Node(value)
#
#     NewNode.next =
